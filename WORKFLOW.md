# 固定工作流

```text
实验完成 → 结果审查 → 假说判定 → 风险审查 → 更新研究路线 → 发布新版本（Vx.y）
```

任何一步都不能跳过；没有结果审查就不做假说判定，没有风险审查就不改路线。

## 第 1 步：结果审查（Result Review）

检查清单：

- [ ] 脚本、运行日志、CSV、图是否齐全且可复现？
- [ ] 是否存在数据泄漏（阈值是否只用训练/验证数据确定）？
- [ ] 指标与结论是否一致？有没有把 Interpretation 写成 Observation？
- [ ] 实验契约是否在跑之前就写好（Hypothesis / Dataset / Train / Test / Method / Metrics / Rule）？

产出：一份确认过的实验结果摘要（事实部分）。

## 第 2 步：假说判定（Hypothesis Verdict）

对每条可检验预测给出四选一的判定：

| 判定 | 含义 |
| --- | --- |
| 支持 | 受控实验支持，且排除了主要替代解释 |
| 部分支持 | 方向一致，但存在替代解释或证据强度不足 |
| 不支持 | 数据与预测不一致 |
| 无法判定 | 统计功效不足 |

同时标注证据等级：A（本数据集内受控证实）/ B（一致但需确认）/ C（尚未成立）/ D（未知）。

## 第 3 步：风险审查（Risk Review）

必查：

1. 样本量与统计功效（训练/测试窗口数、正常文件数）。
2. 样本独立性（重叠窗口、嵌套特征集、同一文件内的相关性）。
3. 多重比较（是否校正；未校正 p 值只能作描述性）。
4. 事后观察（post-hoc）与预先定义假设的区别。
5. 泛化风险（单数据集、单传感器、单划分）。

产出：风险清单 + 每条风险对结论的影响。

## 第 4 步：更新研究路线（Route Update）

- Conference Track：核心故事是否仍然成立？是否达到投稿门槛？
- Journal Track：下一个最有信息量的实验是什么？为什么是它？
- 若结论被削弱：明确撤回或降级哪些表述。

## 第 5 步：发布新版本（Version Release）

每条线各自发布 `RESEARCH_PLAN_vX.Y.md`，并在 `CHANGELOG.md` 记录六类变更：

**保留 / 删除 / 修改 / 新增 / 延后 / 因实验结果而改变**

新版本必须回答六件事：

1. 已确认事实
2. 被否定或弱化的假设
3. 当前最可信机制
4. 当前最大科研风险
5. 下一实验及理由（Conference 投稿门槛 / Journal 下一步）
6. 论文故事如何随证据变化

## 纪律

- 不预先决定下一版内容。
- 不为保住故事而追加实验。
- 失败结果是结果，不是问题。
