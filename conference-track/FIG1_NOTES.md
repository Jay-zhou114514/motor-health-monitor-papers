# Fig. 1 说明与检查状态

文件：`figures/fig1_three_faces.png`（3100 × 1120 px，200 dpi，由 `src/make_fig1_concept.py` 生成）

## 三个面板

| 面板 | 对应主张 | 关键数字 |
| --- | --- | --- |
| (a) Scope | C8（EXP-V2-03） | within-bearing **0.00%** vs bearing-level holdout **40.63%**（折 0.4–97.1%） |
| (b) Units | C9（EXP-V2-05/06） | 固定 20 条记录：k=1 时 SD 23–38 pp、均值误报 54–58%；k=4 时 SD 8–14 pp；3σ RMS 与 iForest 各 6/6；**马氏饱和不可检验** |
| (c) Definition | C10（EXP-V2-04） | 5 组健康阶段规则的 SD 极差：PRONOSTIA **2.40 pp**（干净）、XJTU-SY **5.19 pp**（含纳入混杂） |

## 检查状态（**重要**）

| 检查 | 状态 |
| --- | --- |
| 程序化：文本越界 | ✅ 49 个文本元素，**0 个越界**（画布 1550×560 pt） |
| 程序化：输出尺寸 | ✅ 15.5×5.6 in @ 200 dpi = 3100×1120 px |
| **人工视觉检查** | ❌ **未做**——生成方无法查看图像 |

**规则**：本图在进入投稿稿之前，**必须由项目负责人做一次视觉检查**，确认：
① 三个面板的示意图是否表意清楚；② 是否有元素重叠；③ 字号在印刷尺寸下是否可读；
④ 配色在灰度打印下是否仍可区分。

检查通过后，请把本文件的状态行改为 "已通过（日期）"。